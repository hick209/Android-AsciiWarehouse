<?xml version="1.0" encoding="utf-8"?>
<menu
	xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
	xmlns:tools="http://schemas.android.com/tools"
	tools:context=".store.WarehouseListActivity">

	<item
		android:id="@+id/warehouse_search"
		android:title="@string/warehouse_menu_search"
		android:icon="@drawable/ic_search"
		app:actionViewClass="android.support.v7.widget.SearchView"
		app:showAsAction="ifRoom"/>

	<item
		android:id="@+id/warehouse_stockOnly"
		android:checkable="true"
		android:checked="false"
	    android:title="@string/warehouse_menu_onlyInStock"
	    android:titleCondensed="@string/warehouse_menu_onlyInStock_condensed"
		app:showAsAction="never"/>

</menu>
